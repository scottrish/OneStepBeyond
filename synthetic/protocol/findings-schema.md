# Synthetic Persona Findings Schema

Each assessment must produce a machine-readable `findings.yaml` file in addition
to the human-readable report.

## File location

Use the output directory defined by the assessment.

Example:

```text
synthetic/reports/<assessment-id>/
├── report.md
├── findings.yaml
├── transcript.md
└── screenshots/
```
